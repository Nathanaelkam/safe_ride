# Core API
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0

# Async PostgreSQL
sqlalchemy[asyncio]>=2.0.30,<3.0.0
asyncpg>=0.29.0

# Auth utilities
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]>=3.3.0

# Configuration
pydantic-settings>=2.3.0

# Redis (for future services)
redis>=5.1.0
aioredis>=2.0.0   # async Redis client

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0

httpx-ws

#   metrics
prometheus-client>=0.20.0

# (Optional) voice processing for emergency service
# librosa, SpeechRecognition – to be added by Mael